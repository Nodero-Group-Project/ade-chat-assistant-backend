# Query API

To start LLM server first install ollama if not already:
```
irm https://ollama.com/install.ps1 | iex
```

then pull the model:
```
ollama pull qwen2.5:7b-instruct
```

To start the LLM server:
```
ollama run qwen2.5:7b-instruct
```
